from fastapi import APIRouter

router = APIRouter(tags=["coaching"])

# TODO (Member 2, outcome 6): GET /api/coaching, backed by
# data/coaching_events.csv. See PLAN.md "Real-time operator coaching".
