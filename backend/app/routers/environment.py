from fastapi import APIRouter

router = APIRouter(tags=["environment"])

# TODO (Member 3, outcome 8): GET /api/environment, backed by
# data/environment.csv. See PLAN.md "Operator health & environmental monitoring".
